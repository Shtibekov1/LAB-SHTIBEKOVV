
# Лабораторная работа №2 — DOM и события
**Автор:** Штибеков Муслим Тимурович
**Тема:** Основы синтаксиса и базовые конструкции языка JavaScript  
**Цель:** Ознакомление с типами данных, функциями, объектами, массивами и циклами. Приобретение практических навыков написания кода, работы с консолью браузера и инструментами разработчика.

---

## Готовый сайт  
🔗 https://js-yukachka.onrender.com

---

## Описание проделанной работы

В проекте реализовано 5 задач, каждая из которых демонстрирует разные техники работы с DOM:

### 1. Задача «Like»
Кнопка, которая умеет переключаться между активным и неактивным состоянием, меняя текст и статус.

### 2. Задача «Like / Dislike»
Две кнопки, исключающие друг друга. Невозможно активировать обе одновременно.

### 3. Задача «Корзина»
- Карточки товаров с изображениями.  
- Кнопки «В корзину».  
- Иконка корзины в шапке с динамическим счётчиком.  
- Полная работа через DOM API (без innerHTML).

### 4. Сортировка массива
- Генерация массива случайных чисел.  
- Три кнопки: по возрастанию, по убыванию, исходный порядок.  
- Список создаётся только через `createElement`.

### 5. Координаты клика
Выводит X/Y координаты клика и тег элемента, по которому кликнули.

---


## Вопросы для защиты

### 1. Назначение `.gitignore` и ESLint  
**.gitignore** — файл, скрывающий временные и служебные файлы от Git.  
**ESLint** — инструмент для анализа JS-кода, помогает поддерживать единый стиль и избегать ошибок.

---

### 2. window, DOM, BOM  
- **window** — глобальный объект браузера.  
- **DOM** — структура HTML в виде дерева объектов.  
- **BOM** — объекты браузера: `navigator`, `location`, `history`.

---

### 3. DOM‑дерево, навигация и поиск  
Навигация по дереву:  
`parentNode`, `children`, `firstChild`, `nextElementSibling`.  
Поиск элементов:  
`getElementById`, `querySelector`, `querySelectorAll`.

---

### 4. Классы DOM‑узлов  
Основные типы:  
- `Node`  
- `Element`  
- `HTMLElement`  
- `Text`  
- `Document`  
- `Comment`  

---

### 5. Изменение документа  
Создание элементов:

```js
const div = document.createElement("div");
```

Добавление:

```js
parent.append(div);
```

Удаление:

```js
elem.remove();
```

Редактирование:

```js
elem.textContent = "Привет";
elem.classList.toggle("active");
```

---

### 6. События и обработчики  
Назначение обработчика:

```js
button.addEventListener("click", handleClick);
```

`event` содержит данные о событии.

---

### 7. Простые UI‑события  
- `click`  
- `input`  
- `change`  
- `mousemove`  
- `keydown`  

---

### 8. Примеры свойств `MouseEvent`  
- `clientX`, `clientY` — координаты клика  
- `target` — элемент, на который нажали  
- `buttons` — информация о нажатых кнопках мыши  
- `pageX`, `pageY` — координаты относительно страницы  

---

